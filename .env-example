# API URLs
VITE_API_URL=http://localhost:8080

# For mobile development: create .env.local with your machine's IP
# Find your IP: ifconfig | grep "inet " | grep -v 127.0.0.1 | awk '{print $2}' | head -1
# VITE_CAPACITOR_API_URL=http://192.168.0.XXX:8080

# Cognito Configuration
VITE_COGNITO_USER_POOL_ID=<pool_id>
VITE_COGNITO_CLIENT_ID=<client_id>
